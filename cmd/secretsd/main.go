// Command secretsd is the server of secrets-spec.md §8. M1 is the offline
// milestone, so the server has no subcommand yet; `serve`, tokens, sync and
// the rest arrive with M2.
package main

func main() {}
